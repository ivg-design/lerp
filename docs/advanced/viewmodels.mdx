---
sidebar_position: 3
title: ViewModels
description: Binding scripts to ViewModels for reactive data-driven animations
---

import Quiz from '@site/src/components/Quiz';
import Exercise from '@site/src/components/Exercise';

# ViewModels

## Rive Context: ViewModels Connect Data to Graphics

ViewModels are Rive's data binding system. They let you expose properties (numbers, strings, booleans, colors, enums, lists) that can be controlled from:

- The Rive runtime (your app)
- State machines
- Other Node Scripts

**Key concepts:**
- ViewModels define a **schema** of properties
- Properties are **reactive**: changes propagate automatically
- Scripts can **read**, **write**, and **listen** to ViewModel properties
- Use `context:viewModel()` for the artboard's main ViewModel
- Use `Input<Data.X>` for nested ViewModel references

---

## Accessing the Main ViewModel

Every artboard can have a ViewModel bound to it. Access it through the `context` parameter in `init`.

```lua
--!strict

export type VMReader = {}

function init(self: VMReader, context: Context): boolean
    local vm = context:viewModel()
    if vm then
        print("ViewModel found!")
    else
        print("No ViewModel bound to this artboard.")
    end
    return true
end

function draw(self: VMReader, renderer: Renderer)
end

return function(): Node<VMReader>
    return {
        init = init,
        draw = draw,
    }
end
```

---

## ViewModel Property Access Methods

| Method | Returns | Description |
|--------|---------|-------------|
| `vm:getNumber(name)` | `Property<number>?` | Get a numeric property |
| `vm:getString(name)` | `Property<string>?` | Get a string property |
| `vm:getBoolean(name)` | `Property<boolean>?` | Get a boolean property |
| `vm:getColor(name)` | `Property<Color>?` | Get a color property |
| `vm:getEnum(name)` | `Property<string>?` | Get an enum property (returns string) |
| `vm:getList(name)` | `List?` | Get a list property |
| `vm:getTrigger(name)` | `Trigger?` | Get a trigger |

All getters return `nil` if the property doesn't exist. Always check before using.

---

<Exercise
  id="advanced-viewmodel-1"
  title="Exercise 1: Read and Display ViewModel Data"
  difficulty={2}
  hint="Use context:viewModel() to get the VM, then vm:getNumber('score') to get the property. Access .value to read the current value."
  expectedAnswer="viewmodel"
  explanation="ViewModels provide reactive properties that scripts can read. Use context:viewModel() in init, then access properties with getNumber(), getString(), etc."
  task="Complete the init function to get the ViewModel and score property, then read its value."
  expectedOutput={`ViewModel connected!
Score property found: 50
ANSWER: viewmodel`}
  code={`--!strict
-- Read a score property from the main ViewModel

export type ScoreDisplay = {
    vm: ViewModel?,
    scoreProp: Property<number>?,
    path: Path,
    paint: Paint,
    barWidth: number,
}

function init(self: ScoreDisplay, context: Context): boolean
    self.barWidth = 0

    -- TODO 1: Get the ViewModel using context:viewModel()
    self.vm = nil  -- Replace with context:viewModel()

    if self.vm then
        print("ViewModel connected!")

        -- TODO 2: Get the score property using vm:getNumber("score")
        self.scoreProp = nil  -- Replace with self.vm:getNumber("score")

        if self.scoreProp then
            -- TODO 3: Read the value and set barWidth = value * 2
            print(\`Score property found: {self.scoreProp.value}\`)
            print("ANSWER: viewmodel")
        end
    end

    self.path = Path.new()
    self.paint = Paint.with({ style = "fill", color = Color.rgb(80, 200, 120) })

    return true
end

function advance(self: ScoreDisplay, seconds: number): boolean
    if self.scoreProp then
        self.barWidth = self.scoreProp.value * 2
    end
    return true
end

function draw(self: ScoreDisplay, renderer: Renderer)
    self.path:reset()
    self.path:moveTo(Vector.xy(-100, -10))
    self.path:lineTo(Vector.xy(-100 + self.barWidth, -10))
    self.path:lineTo(Vector.xy(-100 + self.barWidth, 10))
    self.path:lineTo(Vector.xy(-100, 10))
    self.path:close()

    renderer:drawPath(self.path, self.paint)
end

return function(): Node<ScoreDisplay>
    return {
        init = init,
        advance = advance,
        draw = draw,
        vm = nil,
        scoreProp = nil,
        path = late(),
        paint = late(),
        barWidth = 0,
    }
end`}
  solution={`-- TODO 1: Get the ViewModel
self.vm = context:viewModel()

-- TODO 2: Get the score property
self.scoreProp = self.vm:getNumber("score")

-- TODO 3: Read the value
self.barWidth = self.scoreProp.value * 2`}
>
**Premise:** You're building a score display that reads data from the artboard's ViewModel.

**Use Case:** ViewModels let external code (your app, state machines) drive animation data. Score displays, health bars, and progress indicators all use this pattern.
</Exercise>

**Editor setup:**
1. Create a ViewModel with a `score` number property (default: 50)
2. Bind the ViewModel to your artboard

:::warning Path Reset Location
In this example, `path:reset()` is in `draw` for simplicity. For production code, move path updates to `advance` to avoid potential rendering artifacts.
:::

**Key points:**
- Access `.value` to read the property's current value
- Store the property reference to avoid repeated lookups
- Check for `nil` before using properties

---

<Exercise
  id="advanced-viewmodel-2"
  title="Exercise 2: Writing to ViewModel Properties"
  difficulty={2}
  hint="Assign to .value to write: self.xProp.value = newValue. Use sine/cosine with time for smooth oscillation."
  expectedAnswer="writing"
  explanation="ViewModel properties are read/write. Assign to .value to update them—changes automatically propagate to all bound visual elements."
  task="Complete the advance function to animate the position properties using sine and cosine waves."
  expectedOutput={`Position driver initialized
Driving position...
ANSWER: writing`}
  code={`--!strict
-- Drive ViewModel position properties from script

export type PositionDriver = {
    vm: ViewModel?,
    xProp: Property<number>?,
    yProp: Property<number>?,
    time: number,
}

function init(self: PositionDriver, context: Context): boolean
    self.time = 0
    self.vm = context:viewModel()

    if self.vm then
        self.xProp = self.vm:getNumber("posX")
        self.yProp = self.vm:getNumber("posY")
        print("Position driver initialized")
    end

    return true
end

function advance(self: PositionDriver, seconds: number): boolean
    self.time += seconds

    -- TODO 1: Write to xProp.value using sin(time) * 100
    if self.xProp then
        -- self.xProp.value = math.sin(self.time) * 100
    end

    -- TODO 2: Write to yProp.value using cos(time * 1.5) * 50
    if self.yProp then
        -- self.yProp.value = math.cos(self.time * 1.5) * 50
    end

    if math.floor(self.time) == 1 and math.floor(self.time - seconds) < 1 then
        print("Driving position...")
        print("ANSWER: writing")
    end

    return true
end

function draw(self: PositionDriver, renderer: Renderer)
end

return function(): Node<PositionDriver>
    return {
        init = init,
        advance = advance,
        draw = draw,
        vm = nil,
        xProp = nil,
        yProp = nil,
        time = 0,
    }
end`}
  solution={`-- TODO 1: Write to xProp
if self.xProp then
    self.xProp.value = math.sin(self.time) * 100
end

-- TODO 2: Write to yProp
if self.yProp then
    self.yProp.value = math.cos(self.time * 1.5) * 50
end`}
>
**Premise:** You're creating a position driver that controls an element's movement through ViewModel properties.

**Use Case:** Scripts can animate visual elements indirectly by writing to ViewModel properties that are bound to layer transforms, colors, or other visual attributes.
</Exercise>

**Editor setup:**
1. Add `posX` and `posY` number properties to your ViewModel
2. Bind these properties to an element's transform in the artboard

**Key points:**
- Write to `.value` to update the property
- Changes propagate to all bound elements automatically
- ViewModel properties are the bridge between scripts and visual elements

---

<Exercise
  id="advanced-viewmodel-3"
  title="Exercise 3: Listening for Property Changes"
  difficulty={2}
  hint="Use property:addListener(function() ... end) to register a callback. Inside the callback, read the new value and update visuals."
  expectedAnswer="listener"
  explanation="Listeners make scripts reactive to external changes. When the runtime or state machine updates a property, your listener callback fires automatically."
  task="Complete the init function to register a listener that changes color when the mode property changes."
  expectedOutput={`Listener registered for mode
Mode changed to: active
ANSWER: listener`}
  code={`--!strict
-- React to ViewModel property changes with listeners

export type ChangeListener = {
    vm: ViewModel?,
    modeProp: Property<string>?,
    currentMode: string,
    paint: Paint,
    path: Path,
}

function init(self: ChangeListener, context: Context): boolean
    self.currentMode = "idle"
    self.vm = context:viewModel()

    if self.vm then
        self.modeProp = self.vm:getString("mode")
        if self.modeProp then
            self.currentMode = self.modeProp.value

            -- TODO: Register a listener using self.modeProp:addListener(function() ... end)
            -- Inside the callback:
            --   1. Update self.currentMode = self.modeProp.value
            --   2. Print "Mode changed to: {self.currentMode}"
            --   3. If mode == "active", set paint color to red (255, 100, 100)
            --   4. Otherwise set paint color to blue (100, 100, 255)
            --   5. Print "ANSWER: listener" when mode changes

            print("Listener registered for mode")
        end
    end

    self.path = Path.new()
    self.path:moveTo(Vector.xy(-40, -40))
    self.path:lineTo(Vector.xy(40, -40))
    self.path:lineTo(Vector.xy(40, 40))
    self.path:lineTo(Vector.xy(-40, 40))
    self.path:close()

    self.paint = Paint.with({ style = "fill", color = Color.rgb(100, 100, 255) })

    return true
end

function draw(self: ChangeListener, renderer: Renderer)
    renderer:drawPath(self.path, self.paint)
end

return function(): Node<ChangeListener>
    return {
        init = init,
        draw = draw,
        vm = nil,
        modeProp = nil,
        currentMode = "idle",
        paint = late(),
        path = late(),
    }
end`}
  solution={`-- Register the listener
self.modeProp:addListener(function()
    self.currentMode = self.modeProp.value
    print(\`Mode changed to: {self.currentMode}\`)
    if self.currentMode == "active" then
        self.paint.color = Color.rgb(255, 100, 100)
    else
        self.paint.color = Color.rgb(100, 100, 255)
    end
    print("ANSWER: listener")
end)`}
>
**Premise:** You're building a reactive component that changes appearance when a ViewModel property changes.

**Use Case:** Listeners enable event-driven behavior—changing states, triggering animations, or updating visuals when external code modifies ViewModel properties.
</Exercise>

**Editor setup:**
1. Add a `mode` string property to your ViewModel (default: "idle")
2. Use a state machine or runtime code to change it to "active"

**Key points:**
- `addListener()` registers a callback that fires on property changes
- Listeners persist for the lifetime of the script
- Use listeners for event-driven logic (mode changes, state transitions)

---

<Exercise
  id="advanced-viewmodel-4"
  title="Exercise 4: Firing Triggers"
  difficulty={2}
  hint="Use vm:getTrigger('name') to get a trigger. Register listener with :addListener(function() ... end). Fire with :fire()."
  expectedAnswer="trigger"
  explanation="Triggers are one-shot events with no persistent value. Use them for notifications like collisions, button presses, or animation completions."
  task="Complete the init function to get the trigger, register a listener, and implement a pointerDown handler to fire it."
  expectedOutput={`Trigger listener registered
Hit trigger fired! Count: 1
Hit trigger fired! Count: 2
ANSWER: trigger`}
  code={`--!strict
-- Use triggers for one-shot events

export type TriggerDemo = {
    vm: ViewModel?,
    hitTrigger: Trigger?,
    clickCount: number,
    path: Path,
    paint: Paint,
}

function init(self: TriggerDemo, context: Context): boolean
    self.clickCount = 0
    self.vm = context:viewModel()

    if self.vm then
        -- TODO 1: Get the trigger using self.vm:getTrigger("onHit")
        self.hitTrigger = nil  -- Replace with getTrigger

        if self.hitTrigger then
            -- TODO 2: Register a listener that:
            --   1. Increments clickCount
            --   2. Prints "Hit trigger fired! Count: {count}"
            --   3. Prints "ANSWER: trigger" when count reaches 2

            print("Trigger listener registered")
        end
    end

    self.path = Path.new()
    self.path:moveTo(Vector.xy(-30, -30))
    self.path:lineTo(Vector.xy(30, -30))
    self.path:lineTo(Vector.xy(30, 30))
    self.path:lineTo(Vector.xy(-30, 30))
    self.path:close()

    self.paint = Paint.with({ style = "fill", color = Color.rgb(255, 180, 60) })

    return true
end

function pointerDown(self: TriggerDemo, event: PointerEvent)
    -- TODO 3: Fire the trigger when clicked
    -- if self.hitTrigger then
    --     self.hitTrigger:fire()
    -- end
    event:hit()
end

function draw(self: TriggerDemo, renderer: Renderer)
    renderer:drawPath(self.path, self.paint)
end

return function(): Node<TriggerDemo>
    return {
        init = init,
        pointerDown = pointerDown,
        draw = draw,
        vm = nil,
        hitTrigger = nil,
        clickCount = 0,
        path = late(),
        paint = late(),
    }
end`}
  solution={`-- TODO 1: Get the trigger
self.hitTrigger = self.vm:getTrigger("onHit")

-- TODO 2: Register listener
self.hitTrigger:addListener(function()
    self.clickCount += 1
    print(\`Hit trigger fired! Count: {self.clickCount}\`)
    if self.clickCount == 2 then
        print("ANSWER: trigger")
    end
end)

-- TODO 3: Fire the trigger in pointerDown
if self.hitTrigger then
    self.hitTrigger:fire()
end`}
>
**Premise:** You're building a clickable component that fires a trigger event when pressed.

**Use Case:** Triggers communicate events between scripts, state machines, and runtime code—perfect for buttons, collision notifications, and animation cues.
</Exercise>

**Editor setup:**
1. Add an `onHit` trigger to your ViewModel
2. Optionally connect the trigger to a state machine transition

**Key points:**
- Triggers are one-shot events with no persistent value
- Use `getTrigger()` to access, `fire()` to activate, `addListener()` to respond
- Common uses: collision events, button presses, animation completion signals

---

<Exercise
  id="advanced-viewmodel-5"
  title="Exercise 5: Nested ViewModel Inputs"
  difficulty={3}
  hint="Use Input<Data.YourViewModelName> in the type. Access properties like self.character.posX.value. Use late() in the factory."
  expectedAnswer="nested"
  explanation="Input<Data.X> binds to a specific ViewModel instance assigned in the editor. Access its properties directly using dot notation."
  task="Complete the advance function to animate the character's position using the nested ViewModel properties."
  expectedOutput={`Character controller ready
Animating character...
ANSWER: nested`}
  code={`--!strict
-- Control a nested ViewModel using script inputs

export type CharacterController = {
    character: Input<Data.Character>,
    speed: Input<number>,
    time: number,
}

function init(self: CharacterController): boolean
    self.time = 0
    print("Character controller ready")
    return true
end

function advance(self: CharacterController, seconds: number): boolean
    self.time += seconds

    -- TODO 1: Calculate horizontal offset using sin(time * speed) * 80
    -- TODO 2: Write to self.character.posX.value
    -- TODO 3: Calculate vertical offset using cos(time * speed * 0.7) * 40
    -- TODO 4: Write to self.character.posY.value

    if math.floor(self.time) == 1 and math.floor(self.time - seconds) < 1 then
        print("Animating character...")
        print("ANSWER: nested")
    end

    return true
end

function draw(self: CharacterController, renderer: Renderer)
end

return function(): Node<CharacterController>
    return {
        init = init,
        advance = advance,
        draw = draw,
        character = late(),
        speed = 2.0,
        time = 0,
    }
end`}
  solution={`-- TODO 1-4: Animate character position
local offset = math.sin(self.time * self.speed) * 80
self.character.posX.value = offset
self.character.posY.value = math.cos(self.time * self.speed * 0.7) * 40`}
>
**Premise:** You're building a character controller that drives a nested ViewModel's position properties.

**Use Case:** Nested ViewModels let you create reusable components (characters, UI elements) that scripts can control independently.
</Exercise>

**Editor setup:**
1. Create a `Character` ViewModel with `posX` and `posY` number properties
2. Bind the ViewModel to an element in your artboard
3. In the script's input panel, assign the ViewModel instance to the `character` input

**Key points:**
- `Input<Data.X>` where X is your ViewModel name
- Use `late()` because the value is assigned in the editor
- Access nested properties directly: `self.character.posX.value`

---

## ViewModel vs Script Inputs

| Feature | `context:viewModel()` | `Input<Data.X>` |
|---------|----------------------|-----------------|
| Scope | Artboard-level (global) | Per-instance |
| Setup | Automatic | Requires editor binding |
| Use case | Shared state across scripts | Component-specific data |
| Access | In `init` via context | Available immediately |

---

## Knowledge Check

<Quiz
  question="How do you access the artboard's main ViewModel?"
  id="viewmodels-q1"
  options={[
    { text: "self.viewModel" },
    { text: "context:viewModel()", correct: true },
    { text: "ViewModel.get()" },
    { text: "context.vm" },
  ]}
  explanation="Use context:viewModel() in the init function to access the ViewModel bound to the artboard."
/>

<Quiz
  question="What does property:addListener(callback) do?"
  id="viewmodels-q2"
  options={[
    { text: "Replaces the property value" },
    { text: "Registers a function to call when the property changes", correct: true },
    { text: "Returns the property's current value" },
    { text: "Removes the property from the ViewModel" },
  ]}
  explanation="addListener() registers a callback function that fires whenever the property's value changes, enabling reactive behavior."
/>

<Quiz
  question="How do you read the current value of a ViewModel property?"
  id="viewmodels-q3"
  options={[
    { text: "property.get()" },
    { text: "property.value", correct: true },
    { text: "property:read()" },
    { text: "property()" },
  ]}
  explanation="Access the .value property to read the current value. Use the same property to write: property.value = newValue"
/>

<Quiz
  question="When should you use late() for an input?"
  id="viewmodels-q4"
  options={[
    { text: "When the input has a default value" },
    { text: "When the input will be assigned in the Rive editor", correct: true },
    { text: "When the input is optional" },
    { text: "When the input changes frequently" },
  ]}
  explanation="late() indicates that the input doesn't have a value at construction time but will be assigned in the editor before init runs."
/>

<Quiz
  question="What is the difference between a Property and a Trigger?"
  id="viewmodels-q5"
  options={[
    { text: "Properties are read-only, Triggers are write-only" },
    { text: "Properties hold persistent values, Triggers are one-shot events", correct: true },
    { text: "Triggers are faster than Properties" },
    { text: "There is no difference" },
  ]}
  explanation="Properties maintain a value that can be read and written. Triggers have no persistent value - they simply fire events that listeners can respond to."
/>

---

## Key Takeaway

ViewModels are the bridge between your animation data and your scripts. Use `context:viewModel()` for artboard-level shared data, and `Input<Data.X>` with `late()` for component-specific bindings. Listeners make your scripts reactive to external changes.

---

## Next Up

**[Procedural Geometry ->](./procedural)**
